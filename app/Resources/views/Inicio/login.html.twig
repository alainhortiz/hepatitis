<!DOCTYPE html>
<html lang="es">

  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="shortcut icon" href="{{ asset('images/LogoHepatitis.png') }}">

    <title>Hepatitis</title>

      <link rel="stylesheet" href="{{ asset('easyadmin/stylesheet/login/bootstrap.min.css') }}"/>
      <link rel="stylesheet" href="{{ asset('easyadmin/stylesheet/login/bootstrap-theme.css') }}"/>
      <link rel="stylesheet" href="{{ asset('easyadmin/stylesheet/login/style.css') }}"/>
      <link rel="stylesheet" href="{{ asset('easyadmin/stylesheet/login/elegant-icons-style.css') }}"/>
  </head>

  <body class="login-img3-body">
    <div class="container contenedorLogin">
      <div>
        <h3 class="text-center" style="margin-bottom: 0">Registro Nacional de </h3>
        <h1 style="font-weight: bold;  color: #70cbb1" class="text-center">H E P A T I T I S</h1>
      </div>
      <form id="loginForm" class="login-form" method="post" action="{{ path('login_check') }}">
        <div class="login-wrap">
          <p style="text-align: center">
            <img src="{{ asset('images/LogoHepatitis.png')}}" style="width: 100px; margin-bottom: 20px">
          </p>
          {% if error %}
            <div class="alert alert-danger">
              <p>
                <strong>Credenciales inválidas</strong>
              </p>
            </div>
          {% endif %}
          <div class="input-group">
            <span class="input-group-addon"><i class="icon_profile"></i></span>
            <input type="text" class="form-control" placeholder="Usuario" name="_username" value="{{ last_username }}" id="username" autofocus required>
          </div>
          <div class="input-group">
            <span class="input-group-addon"><i class="icon_key_alt"></i></span>
            <input type="password" class="form-control" placeholder="Contraseña" name="_password" id="password" required>
          </div>
          <input type="hidden" name="_target_path" value="/inicio" />

          <button class="btn btn-primary btn-lg btn-block" type="submit">ACEPTAR</button>
        </div>
      </form>
    </div>
  </body>

</html>
